public class Main{
    public static void main(String[] args){

        // if durumunun kısaltması condition

        // int x = 1;
        // String evenOrOdd =  (x % 2 == 0) ? "The numnber is Even" : "The number is Odd";
        // System.out.print(evenOrOdd);


        // int hours = 13;
        // String timeOfDay =  (hours >12) ? "P.M."  : "A:M.";
        // System.out.print(timeOfDay);


        int income = 20000;

        double textRate = (income >= 40000) ? 0.25 : 0.15;
        System.out.print(textRate);




    }
}